# 复制本文件为 .env.deploy.local 后填写，不要提交真实服务器信息。
export ALIYUN_HOST="你的服务器IP或域名"
export ALIYUN_USER="root"
export ALIYUN_PORT="22"
export REMOTE_ROOT="/www/wwwroot/diandu"
export PHP_FPM_SERVICE="php-fpm-72.service"

# 1 同步 PHP 控制器并重启 php-fpm；0 只同步 dist 静态文件。
export DEPLOY_CONTROLLERS="1"
